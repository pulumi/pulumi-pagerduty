// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Use this data source to look up a specific [v3 schedule](https://developer.pagerduty.com/api-reference/d90c4c94e3ce2-create-a-schedule) by name so you can reference its ID in other resources such as escalation policies.
 *
 * > **Note:** This data source requires the `flexible-schedules-early-access` early access flag on your PagerDuty account. The required `X-Early-Access` header is sent automatically by the provider.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as pagerduty from "@pulumi/pagerduty";
 *
 * const oncall = pagerduty.getSchedulev2({
 *     name: "Engineering On-Call",
 * });
 * const example = new pagerduty.EscalationPolicy("example", {
 *     name: "Engineering Escalation Policy",
 *     numLoops: 2,
 *     rules: [{
 *         escalationDelayInMinutes: 10,
 *         targets: [{
 *             type: "schedule_reference",
 *             id: oncall.then(oncall => oncall.id),
 *         }],
 *     }],
 * });
 * ```
 */
export function getSchedulev2(args: GetSchedulev2Args, opts?: pulumi.InvokeOptions): Promise<GetSchedulev2Result> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("pagerduty:index/getSchedulev2:getSchedulev2", {
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getSchedulev2.
 */
export interface GetSchedulev2Args {
    /**
     * The exact name of the v3 schedule to look up.
     */
    name: string;
}

/**
 * A collection of values returned by getSchedulev2.
 */
export interface GetSchedulev2Result {
    /**
     * The ID of the found schedule.
     */
    readonly id: string;
    /**
     * The name of the found schedule.
     */
    readonly name: string;
}
/**
 * Use this data source to look up a specific [v3 schedule](https://developer.pagerduty.com/api-reference/d90c4c94e3ce2-create-a-schedule) by name so you can reference its ID in other resources such as escalation policies.
 *
 * > **Note:** This data source requires the `flexible-schedules-early-access` early access flag on your PagerDuty account. The required `X-Early-Access` header is sent automatically by the provider.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as pagerduty from "@pulumi/pagerduty";
 *
 * const oncall = pagerduty.getSchedulev2({
 *     name: "Engineering On-Call",
 * });
 * const example = new pagerduty.EscalationPolicy("example", {
 *     name: "Engineering Escalation Policy",
 *     numLoops: 2,
 *     rules: [{
 *         escalationDelayInMinutes: 10,
 *         targets: [{
 *             type: "schedule_reference",
 *             id: oncall.then(oncall => oncall.id),
 *         }],
 *     }],
 * });
 * ```
 */
export function getSchedulev2Output(args: GetSchedulev2OutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetSchedulev2Result> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("pagerduty:index/getSchedulev2:getSchedulev2", {
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getSchedulev2.
 */
export interface GetSchedulev2OutputArgs {
    /**
     * The exact name of the v3 schedule to look up.
     */
    name: pulumi.Input<string>;
}
